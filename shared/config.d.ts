// Types for config.js so that we can use it in TypeScript files
declare module '@config' {
  export const CLIENT_URL: string;
  export const API_PATH: string;
  export const API_URL: string;
  export const SERVER_URL: string;
}
